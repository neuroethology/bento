Bento Configuration Class
=========================

The configuration class BentoConfig handles holding onto user-specific configuration
data, including which experiments database to use, and if it is a public database,
the server, username and credentials needed for access.

.. module:: bento
    :noindex:

.. automodule:: db.bentoConfig
    :members: